#pragma once
#include <stdint.h>
#include <vector>
#include "Led.h"

constexpr unsigned int LedNumber =4;

class LedBoard
{
	public:
		LedBoard (unsigned char ucColumn);
		void Off();
		void On(unsigned char ucLed);
	private:
		std::vector<Led> Leds = {Led0, Led1, Led2, Led3};
		Led *Leds[4];
};
