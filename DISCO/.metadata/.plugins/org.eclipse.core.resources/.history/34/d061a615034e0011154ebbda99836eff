#pragma once
#include "KeyboardTs.h"
#include "ledlcd.h"

class KeyboardTsLcd{
	KeyboardTsLcd(unsigned int uiColumn);
	private:
	unsigned int uiColumn;
	LedLcd Led(uiColumn);
	KeyboardTs MyKeyboardTs(uiColumn);
};
